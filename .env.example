# Docker Compose Environment Variables

# Frontend URL for CORS configuration
FRONTEND_URL=http://localhost:3005
# For production with SSL:
# FRONTEND_URL=https://chess.cflux.org

# Backend API URL for frontend (WebSocket will use ws:// or wss:// automatically)
VITE_API_URL=http://localhost:3004
# For production with SSL (WebSocket will use wss://):
# VITE_API_URL=https://api.chess.cflux.org

# Allowed hosts for Vite dev server (comma-separated)
VITE_ALLOWED_HOSTS=chess.cflux.org
# For production, add your domain:
# VITE_ALLOWED_HOSTS=chess.cflux.org,www.chess.cflux.org
